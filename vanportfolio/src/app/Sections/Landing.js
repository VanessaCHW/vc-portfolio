"use client";
import React from "react";
import styled from "styled-components";

const Landing = () => {
  return (
    <Container>
      <div>Landing Screen test</div>
    </Container>
  );
};

export default Landing;

const Container = styled.div`
  color: pink;
  background-color: white;
`;
